# ==============================================================================
# 嘉立创EDA 专业版 (LCEDA Pro) 工程 —— 版本控制排除规则
# 远端备份：github.com/changjk1982/workbuddy_github_test1
#
# 判定一条规则该不该进这里的标准（顺序即优先级）：
#   1. 可由源文件随时重新生成的      → 排除（缓存 / 网表 / 钻孔 / 导出件）
#   2. 代表设计意图、无法再生的      → 必须入库（原理图 / PCB / 符号 / 封装 / 脚本）
#   3. 随会话变化且无信息量的机器状态 → 排除（窗口布局 / 自动保存 / 日志）
#      —— 否则每次提交都夹带一堆无意义 diff，真正的改动会被淹没
# ==============================================================================


# ------------------------------------------------------------------------------
# 1. EDA 临时与缓存目录
# ------------------------------------------------------------------------------
# EDA 在保存、自动保存、生成网表时会反复重建这些目录。
# 它们体积大、内容随操作变动，且可能内嵌本机绝对路径（含用户名），
# 入库会导致跨机器检出后路径失配。
_tmp/
tmp/
.eda_tmp/
.eda_cache/
/CACHE/
/cache/
/LCEDA-Pro/
/JLCEDA-Pro/


# ------------------------------------------------------------------------------
# 2. 自动保存与备份文件
# ------------------------------------------------------------------------------
# EDA 的自动保存快照随时被覆盖，入库只会污染提交历史，对回滚毫无帮助
# （真要回滚，靠 git 自己的历史即可）。
*.autosave
*.autosave.*
*.bak
*.bak[0-9]*
*.backup
*~
*.orig
*.rej
*.swp
*.swo


# ------------------------------------------------------------------------------
# 3. EDA 中间产物（从源文件可再生成）
# ------------------------------------------------------------------------------
# 网表：原理图一改就失效，入库必然与源文件不同步，是最典型的"伪版本"文件
*.net
*.NET
*.xml.net

# 钻孔文件
*.drl
*.DRL

# 坐标 / 贴片位置文件：按 EDA 的实际命名模式精确排除。
# 为什么不用裸的 *.csv：BOM 普遍也是 CSV 格式，而 BOM 含选型、替代料、
# 单价等人工决策，属于必须入库的设计意图。一条 *.csv 会把它一起误杀
# （本文件首次实测就踩了这个坑），所以宁可按文件名前缀逐一列举。
*.pos
*_pos.csv
*-pos.csv
*_positions.csv
*_placement.csv
PickAndPlace*.csv
pick_and_place*.csv

# 旧版/中间导出格式
*.tmp
*.temp
*.cache

# ==============================================================================
# ★ 绝对不可排除的文件（防呆说明，本身不是规则）
# ------------------------------------------------------------------------------
# *.epro2  嘉立创EDA专业版的工程源文件（zip 容器，内含原理图+PCB+符号+封装）
#          —— 它是本仓库的核心资产，代表全部不可再生的设计意图。
#          切勿添加 *.zip / *.* 之类宽泛规则把它误杀。
#          保险做法：在 .gitattributes 中以 binary 处理（已配置）。
# BOM/*.csv 逐位号清单与按料号汇总 —— 含选型决策，属设计意图。
#           注意不要引入裸的 *.csv 规则（详见第 3 节的说明）。
# ==============================================================================


# ------------------------------------------------------------------------------
# 4. 制造输出目录（gerber / 装配图 / 导出包）
# ------------------------------------------------------------------------------
# 这些是"产物"不是"源"，一次导出几十个文件，把仓库撑大且 diff 不可读。
# 需要留历史快照的话，正确的做法是打 git tag（如 v1.0-gerber），
# 而不是每次导出都提交一遍。
# >>> 若你确实习惯把 gerber 一并纳入版本控制，删掉本节四行即可 <<<
/Gerber/
/gerber/
/Manufacture/
/Export/
/export/
*.gbr
*.gko
*.gtl
*.gbl
*.gts
*.gbs
*.gto
*.gbo


# ------------------------------------------------------------------------------
# 5. BOM 与贴片文件
# ------------------------------------------------------------------------------
# 注意：BOM 是"手工维护的物料决策"，与网表不同——它含有选型、替代料、
# 单价等人工信息，属于设计意图，因此【不排除】。
# 仅排除 EDA 自动生成、每次都会覆盖的那几类：
*_auto_bom.csv
*_plc.csv


# ------------------------------------------------------------------------------
# 6. 操作系统与编辑器
# ------------------------------------------------------------------------------
Thumbs.db
ehthumbs.db
desktop.ini
$RECYCLE.BIN/
.DS_Store
.idea/
.vs/
*.code-workspace


# ------------------------------------------------------------------------------
# 7. 日志
# ------------------------------------------------------------------------------
*.log
*.log.[0-9]*


# ------------------------------------------------------------------------------
# 8. 工具自身
# ------------------------------------------------------------------------------
# WorkBuddy 在此工程内的工作数据（记忆、临时脚本），不属于设计交付物
.workbuddy/


# ==============================================================================
# 陷阱提示（务必了解再改本文件）
#
# git 的排除规则有方向性：一旦某个【父目录】被排除，其下的子文件无法用
# "!文件名" 反选救回。例如 /Export/ 被排除后，!Export/关键文件.txt 是无效的。
# 所以如果将来发现某个必须入库的文件"消失"了，用下面这条命令定位是哪条规则吃掉的：
#
#     git check-ignore -v <文件路径>
#
# 别靠肉眼猜，宽泛的通配符（如 *.csv）很容易误伤。
# ==============================================================================
